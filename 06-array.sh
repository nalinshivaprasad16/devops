#!/bin/bash
#This is called array , insted of single value it cam ehold multiple values
PERSONS=("NALIN" "SHIVA" "PRASAD")
#inside the array we always refer values with the indeint and indent start from 0
echo "First Person: $(PERSONS[0])"
